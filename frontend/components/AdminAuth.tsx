'use client';

// This component is no longer used as we've migrated from Supabase to SQLite
// Kept as a minimal placeholder to avoid breaking imports

export default function AdminAuth() {
  return null;
} 